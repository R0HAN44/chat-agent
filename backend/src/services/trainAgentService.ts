import { ISource } from "../models/Source"

export const handlePDFSource = (source : ISource) => {

}
export const handleWebsiteSource = (source : ISource) => {
    
}
export const handleQNASource = (source : ISource) => {
    
}
export const handleTextSource = (source : ISource) => {
    
}